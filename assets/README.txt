This folder is reserved for local images/icons/audio if you add any later.
SpeakPath currently uses no external image or audio assets — all icons are
plain text/emoji, and speech is generated live by the browser's built-in
text-to-speech (Web Speech API), so nothing needs to be stored here yet.
